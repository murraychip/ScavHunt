version: 1
frontend:
  phases:
    prebuild:
      commands: 
        - yarn install
    build:
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
    - node_modules/**/*
